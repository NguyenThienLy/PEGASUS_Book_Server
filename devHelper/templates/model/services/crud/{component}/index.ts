import { {Name}Service } from './{name}.service'
import { {Name}Helper } from './{name}.helper'
import { {Name}ErrorService } from './{name}.error'

const {name}Service = new {Name}Service()
const {name}Helper = new {Name}Helper()
const {name}ErrorService = new {Name}ErrorService()

export {
    {name}Service,
    {name}Helper,
    {name}ErrorService,
    {Name}Service
}
